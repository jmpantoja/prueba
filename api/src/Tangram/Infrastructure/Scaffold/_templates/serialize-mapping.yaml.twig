{% if entity.endpoint %}
{{ output | full_name }}:
{% else %}
{{ entity | full_name }}:
{% endif %}
    attributes:
        id:
            groups: [{{ entity | read_group }}]

{% for attribute in entity.attributes %}
        {{ attribute.name }}:
            groups: [{{ entity | read_group }}]

{% endfor %}

{{ input | full_name }}:
    attributes:
{% for attribute in entity.attributes %}
        {{ attribute.name }}:
            groups: [{{ entity | write_group }}]

{% endfor %}
